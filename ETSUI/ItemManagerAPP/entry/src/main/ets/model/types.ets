export interface UserInfo {
  avatar: string;
  nickname: string;
  joinDate: string;
}

export interface AppInfo {
  name: string;
  version: string;
  description: string;
  developer: string;
  copyright: string;
}