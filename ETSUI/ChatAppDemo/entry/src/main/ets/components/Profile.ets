/**
 * 登录页面
 * 负责人：待分配
 * 状态：待开发
 */
import router from '@ohos.router'


@Component
export struct Profile {
  build() {
    Column() {
      Text('我页面 - 待实现')
        .fontSize(20)
    }
    .width('100%')
    .height('100%')
  }
}