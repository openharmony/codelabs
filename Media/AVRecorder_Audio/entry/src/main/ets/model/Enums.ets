export enum RecordStatus {
  Idle = 'Idle',
  Recording = 'Recording',
  Paused = 'Paused'
}

export enum PlayerStatus {
  Idle = 'Idle',
  Playing = 'Playing',
  Paused = 'Paused',
  Completed = 'Completed'
}
