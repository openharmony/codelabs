import { DefaultModule, Project } from "@ohos/hvigor-base";
/**
 * 执行module级别的任务
 *
 * @param project
 * @param opts
 */
export declare function executeModuleTask(project: Project, opts: any): Promise<void>;
/**
 * 创建hvigor实例，注册任务并执行
 *
 * @param model module或者project的Hvigor模型
 * @param shouldCheckTask 根据用户配置看是否需要检查任务是否存在
 * @param opts 命令行中的配置
 */
export declare function execute(model: DefaultModule, shouldCheckTask: boolean, opts: any): Promise<void>;
