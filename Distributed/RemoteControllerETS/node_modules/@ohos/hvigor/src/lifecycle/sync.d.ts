import { DefaultModule, Hvigor } from "@ohos/hvigor-base";
/**
 * 执行每个module中注册的sync类型的任务，将对应的数据模型注册到registry中
 *
 * @param model
 * @param registryTask
 * @param vigorInst
 */
export declare function sync(model: DefaultModule, registryTask: Map<string, boolean>, vigorInst: Hvigor): void;
